# claude-skills

Claude Code 用のカスタムスキル集。各スキルは `<skill-name>/SKILL.md` を持つディレクトリとして配置する。

`pdf-booklet` では、Markdown 解釈とページネーションの共通ロジックを `lib/engine.py` が持ち、テンプレートは見た目だけを持つ。両者の境界は `lib/HTML_CONTRACT.md` が定義する。

## レビュー方針

このリポジトリの主産物は SKILL.md という自然言語の手順書であり、レビューの主対象もそこに置く。

### 出力

- レビューコメントとサマリーはすべて日本語で書く。
- 事実(コードや文面から確認できること)と推測を区別し、推測を断定しない。挙動に関する指摘には `file:line` を引用する。
- Nit は最大5件まで。超えた分はサマリーに件数だけ書く。

### Important の基準

🔴 Important は、優先度の高い順に次のもの。

1. SKILL.md の記述の欠陥
   - 手順に実行不能な記述がある、前提条件や必要なツールの記載が抜けている
   - `description` のトリガー条件が曖昧で、発火すべき場面が判別できない
   - 既存スキルの `description` とトリガーが重複し、どちらが呼ばれるか決まらない
   - 手順の途中に確認ステップ(プレビュー、推測箇所の明示)が無く、生成物がそのまま確定される
2. `engine.py` / `scan_h3_orphans.py` のロジック誤り、未処理例外によるクラッシュ
3. `engine.py`・`HTML_CONTRACT.md`・各テンプレートの `style.css` の間の不整合。クラス名や構造の変更が片側にしか入っていない場合

表現・命名・構成の好みは Nit 止まりとする。

### 指摘しない

- `docs/` 配下の画像ファイルと `LICENSE`
- README や SKILL.md の文章表現の好み。事実誤りは除く
