# claude-skills

Claude Code のカスタムスキルを収めたリポジトリ。各スキルは `<skill-name>/SKILL.md` を持つディレクトリとして配置する。

## レビュー方針

- レビューコメント・サマリーはすべて日本語で書く。
- 指摘は「事実(コードから確認できる)」と「推測」を区別して書き、推測を断定しない。
- SKILL.md は自然言語の手順書なので、記述の曖昧さ・手順の抜け・トリガー条件の重複を主に見る。
