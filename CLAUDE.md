# claude-skills

Claude Code 用のカスタムスキル集。各スキルは `<skill-name>/SKILL.md` を持つディレクトリとして配置する。

`pdf-booklet` では、Markdown 解釈とページネーションの共通ロジックを `lib/engine.py` が持ち、テンプレートは見た目だけを持つ。両者の境界は `lib/HTML_CONTRACT.md` が定義する。

## レビュー方針

このリポジトリの主産物は SKILL.md という自然言語の手順書であり、レビューの主対象もそこに置く。

### 出力

- レビューコメントとサマリーはすべて日本語で書く。
- 事実(コードや文面から確認できること)と推測を区別し、推測を断定しない。挙動に関する指摘には `file:line` を引用する。

### 評価レベル

すべての指摘コメントは、本文の先頭に次のいずれかのラベルを置く。ラベルの無いコメントは投稿しない。

**🔴 CRITICAL** — マージ前に必ず直すもの。

- SKILL.md の記述の欠陥。手順に実行不能な記述がある、前提条件や必要なツールの記載が抜けている、`description` のトリガー条件が曖昧で発火すべき場面が判別できない、既存スキルの `description` とトリガーが重複してどちらが呼ばれるか決まらない、手順の途中に確認ステップ(プレビュー、推測箇所の明示)が無く生成物がそのまま確定される
- `engine.py` / `scan_h3_orphans.py` のロジック誤り、未処理例外によるクラッシュ
- `engine.py`・`HTML_CONTRACT.md`・各テンプレートの `style.css` の間の不整合。クラス名や構造の変更が片側にしか入っていない場合
- スキルの `assets/` に置いた雛形と、SKILL.md の手順の不整合。雛形が参照するファイルや前提（`.node-version`、`package.json` の scripts、hook の登録コマンド）を作る手順が書かれていない場合を含む

**🟡 WARNING** — 動作はするが問題が起きうるもの。

- エラー処理の欠落、異常系で黙って不正な出力を出す経路
- SKILL.md と実際の挙動の軽微な乖離
- テンプレート追加時に `README.md` や同梱テンプレート一覧の更新が漏れている

**🔵 INFO** — 任意の改善提案。表現・命名・構成の好み。最大5件まで。超えた分はサマリーに件数だけ書く。

### マージ判定

🔴 CRITICAL が1件でもあればマージ要件を満たさない。🟡 WARNING と 🔵 INFO はマージを妨げない。

### 指摘しない

- `docs/` 配下の画像ファイルと `LICENSE`
- 変更されていない箇所。この PR で触れていないコードの既存の問題は対象外とする
